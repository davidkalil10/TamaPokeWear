name: Build and Release APK

on:
  push:
    tags:
      - 'v*' # Aciona automaticamente quando você cria uma tag (ex: v1.0.0)
  workflow_dispatch: # Permite rodar o workflow manualmente pelo site do GitHub

jobs:
  build:
    name: Build & Release WearOS APK
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Install dependencies
        run: flutter pub get

      - name: Generate Assets
        run: |
          pip install Pillow
          python tools/download_sprites.py
          python tools/generate_sounds.py

      - name: Build APK (Universal - Debug)
        run: flutter build apk --debug

      - name: Create Release and Upload APK
        uses: softprops/action-gh-release@v1
        with:
          files: build/app/outputs/flutter-apk/app-debug.apk
          tag_name: ${{ github.ref_type == 'tag' && github.ref_name || 'latest-manual' }}
          name: Release ${{ github.ref_type == 'tag' && github.ref_name || 'Manual Build' }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
